<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.sample.uhaha.mybatis.TestMapper">

	<select id="selectAllMytable" resultType="MytableVo">
		select * from mytable
	</select>

	<insert id="insertMytable">
		insert into mytable (id, name)
		values (#{id}, #{name})
	</insert>
	
</mapper>